# automated-CI-CD-pipeline
An automated CI/CD pipeline that builds and tests code committed to GitHub, with build status notifications sent to a WebEx space.

Requirements

1. A GitHub repository
2. Jenkins running in a Docker container
3. ngrok for exposing local Jenkins to the internet
4. A WebEx bot for receiving notifications
